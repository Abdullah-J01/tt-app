/**
 * Landing-page "New study bites" deck.
 *
 * The bites themselves are real studybooks from the data layer (`listStudybooks`)
 * — this only fixes how many of them the deck shows, because the page slices the
 * catalogue page to this size and the skeleton must reserve the same height.
 */
export const BITE_COUNT = 4;
